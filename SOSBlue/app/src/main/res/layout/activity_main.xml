<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <!-- ============================================================
         Top App Bar — App Icon · Title · Search · RSS · Menu
         ============================================================ -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/top_bar_background"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingTop="4dp"
        android:paddingEnd="4dp"
        android:paddingBottom="8dp"
        android:elevation="2dp">

        <!-- No app icon — just the Offline-36 title -->

        <TextView
            android:id="@+id/appTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLength="26"
            android:singleLine="true"
            android:text="Offline-36"
            android:textColor="@color/title_text"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Search input (inline, visible when search icon toggled) -->
        <LinearLayout
            android:id="@+id/searchOption"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="4dp"
            android:paddingEnd="0dp"
            android:background="@drawable/bg_input_pill"
            android:visibility="gone">

            <EditText
                android:id="@+id/inputSearch"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@null"
                android:gravity="start|center"
                android:hint="Search conversations..."
                android:textColorHint="@color/input_hint"
                android:imeOptions="actionSearch"
                android:inputType="text"
                android:singleLine="true"
                android:textColor="@color/input_text"
                android:textSize="14sp" />

            <ImageView
                android:id="@+id/closeSearchOptionIcon"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/close"
                android:padding="6dp"
                android:scaleType="centerInside"
                android:clickable="true"
                android:focusable="true"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                app:tint="@color/icon_tint" />
        </LinearLayout>

        <ImageView
            android:id="@+id/searchIcon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="6dp"
            android:scaleType="centerInside"
            android:src="@drawable/search"
            app:tint="@color/icon_tint"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Search" />

        <ImageView
            android:id="@+id/discoverIcon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="6dp"
            android:scaleType="centerInside"
            android:src="@drawable/feed"
            app:tint="@color/primary_red"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="News Feed" />

        <ImageView
            android:id="@+id/threeDotIcon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="6dp"
            android:scaleType="centerInside"
            android:src="@drawable/dot3"
            app:tint="@color/icon_tint"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Menu" />

    </LinearLayout>

    <!-- ============================================================
         Content: Conversation Inbox + Empty State + New Chat FAB
         ============================================================ -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Conversation list (inbox) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/chatRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingBottom="80dp"
            android:paddingTop="4dp" />

        <!-- Empty state (shown when no conversations exist) -->
        <TextView
            android:id="@+id/textStatus"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="No conversations yet."
            android:textColor="@color/icon_tint"
            android:textSize="16sp"
            android:visibility="gone" />

        <!-- New Chat Extended FAB — shows "Start New Chat" text + compose icon -->
        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/newChatFab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="20dp"
            android:clickable="true"
            android:focusable="true"
            android:text="@string/start_new_chat"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:contentDescription="Start New Chat"
            app:icon="@drawable/ic_bottom_chat"
            app:iconTint="@color/white"
            app:backgroundTint="@color/primary_red"
            app:elevation="6dp" />

    </FrameLayout>

</LinearLayout>